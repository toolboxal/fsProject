CREATE TABLE `person` (
	`id` integer PRIMARY KEY NOT NULL,
	`name` text,
	`block` text,
	`unit` text,
	`street` text,
	`contact` text,
	`category` text,
	`remarks` text,
	`date` text,
	`latitude` real,
	`longitude` real,
	`isPrivate` integer
);
